%p#notice= notice
%h1 醫院及其科別
%dl
  %dt 醫院
  %dd= @hospital.name
  %dt phone
  %dd= @hospital.phone
  %dt address
  %dd= @hospital.address
  %dt grade
  %dd= @hospital.grade
  %dt assess
  %dd= @hospital.assess
  %dt 營業中
  %dd= @hospital.on ? "是" : "否"
  %dt 服務
  %dd= @hospital.ss
  %dt 營業時間
  %dd= @hospital.cHours
  %dl
    %dt 評論數
    %dd= @hospital.comments.size
    %dt 推薦數
    %dd= @hospital.comments.where(is_recommend: true).size
    %dt 分數平均
    %dd= @hospital.avg_score
%table
  %thead
    %tr
      %th 醫院科別名稱
      %th 科別分類
      %th{:colspan => "3"}
  %tbody
    - @divisions.each do |div|
      %tr
        %td= div.name
        %td= div.category.name if div.category
        %td= link_to '科別及科別醫師', admin_division_path(div,hospital_id: @hospital.id)
        %td= link_to '修改科別', edit_admin_division_path(div,hospital_id: @hospital.id)
= link_to '新增醫院科別', new_admin_division_path(hospital_id: @hospital.id)
|
= link_to '編輯醫院', edit_admin_hospital_path(@hospital)
|
= link_to '回醫院列表', admin_hospitals_path

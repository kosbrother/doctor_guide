%h1 Doctor Search
= form_for admin_search_doctors_path, method: :get do |f|
  %p
    = f.label "Search for"
    = text_field_tag :q, params[:q]
    = submit_tag "Go", name: nil
%ul
  - @doctors.each do |doctor|
    %li
      %h3
        = link_to doctor.name, controller: "doctors", action: "show", id: doctor.id

<%= form_for(@division) do |f| %>
  <% if @division.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@division.errors.count, "error") %> prohibited this division from being saved:</h2>

      <ul>
      <% @division.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name,"科別名稱" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :category_id,"類別id" %><br>
    <%= f.text_field :category_id %>
  </div>
  ########### 類別id ######### <br>
  <% Category.all.each do |c| %>
    <%= c.name %>:
    <%= c.id %> ,
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
